name: JSON functions
on: push

defaults:
  run:
    shell: bash # ワークフローで使うシェルをまとめて指定

jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${CONTEXT}"
        env:
          CONTEXT: ${{ toJSON(github) }} # githubコンテキストをJSON文字列でダンプ
